<nav class="navbar navbar-expand-sm bg-dark navbar-dark">
    <ul class="navbar-nav">
        <li class="nav-item {{app.request.get('_route') == 'home' ? 'active'}}">
            <a class="nav-link" href="{{ path('home') }}">Accueil</a>
        </li>
        <li class="nav-item {{app.request.get('_route') == 'offres_list' ? 'active'}}">
            <a  class="nav-link" href="{{ path('offres_list') }}">Liste des offres d'emploi</a>
        </li>
    </ul>
</nav>